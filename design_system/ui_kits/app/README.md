# macOS Updates UI Kit

Small reference app surface for future product UI.

## Surface Requirements

- Start with current run status and local inventory freshness.
- Show update layers in the order used by `update_all.sh`.
- Keep private overlay state visible but not exposed.
- Use blue active/progress states.
- Avoid maintenance-cleaner and risk-framing metaphors.
