# Assets

This folder contains source design assets for the macOS Updates identity.

- `macos-updates-mark.svg` is the icon-only update-orbit mark.
- Runtime web assets live in `/public`.

The mark must remain blue. Do not replace it with maintenance-cleaner symbols
or generic utility icons.
